'use strict';

var chai = require('chai');
var expect = chai.expect;
chai.Assertion.includeStack = true;

var solve = require('../src/problem-solver.js');

describe('Solver should solve the train problem', function () {

    // it('Should pass dummy workflow', function (done) {
    //     var train = require('../src/train-reader.js');
    //     var problem = train(4, 5);

    //     solve(problem, function (problem) {
    //         expect(problem.correct).to.equal(true);
    //         done();
    //     });
    // });
});
